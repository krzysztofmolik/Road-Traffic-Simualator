<UserControl x:Class="RoadTrafficConstructor.Presenters.BuildMode.Blocks.ConnectObject.EditSelectedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:Editors="clr-namespace:RoadTrafficConstructor.Presenters.BuildMode.Blocks.Editors"
             mc:Ignorable="d"
             d:DesignHeight="300"
             d:DesignWidth="300"
             d:DataContext="{d:DesignInstance Type=Editors:CarInserterEditorViewModel}">
    <Grid>
        <Border Background="{StaticResource ControlBackgroundBrush}">
            <ContentControl x:Name="ActiveControl" />
        </Border>
    </Grid>
</UserControl>
